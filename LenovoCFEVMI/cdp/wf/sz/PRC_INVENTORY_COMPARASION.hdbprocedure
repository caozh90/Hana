PROCEDURE "SZEBGVMI"."cdp.wf.sz::PRC_INVENTORY_COMPARASION" ( 
	iv_id VARCHAR2(10), OUT on_exitcode NUMBER
) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY DEFINER 
	DEFAULT SCHEMA SZEBGVMI
	--READS SQL DATA 
	AS
BEGIN
    --Not existing yet
	CALL "cdp.szebgvmi.procedures.pkg_report::PRC_INVENTORY_COMPARASION"(:iv_id,on_exitcode);

END;
