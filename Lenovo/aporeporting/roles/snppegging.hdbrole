role aporeporting.roles::snppegging {
	
	application privilege: aporeporting::snppegging;
	
	sql object aporeporting.model.SNP010:CV_SNP010_PEGGING_REPORT.calculationview: SELECT;
	
	sql object aporeporting.model.SNP010:AT_SNP010_VERSION.attributeview: SELECT;
	
	sql object aporeporting.model:CV_CALWEEK.calculationview: SELECT;
	
}